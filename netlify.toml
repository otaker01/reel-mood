[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: client routes like /discover and /movie/:id must rewrite to
# index.html so a refresh does not 404 on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
